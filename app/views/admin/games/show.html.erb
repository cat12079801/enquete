<p id="notice"><%= notice %></p>

<h1><%= @game.name %></h1>

<div><%= image_tag(@game.image_url, width: 600) if @game.image.present? %></div>

<h3><%= @game.author.first if @game.author.present? %></h3>

<%= link_to 'Edit', edit_admin_game_path(@game) %> |
<%= link_to 'Back', admin_games_path %>
